package projectpack;

import java.io.*;

public class Weather{
	
	int date;
	double PAR;
	static double SRAD=0, Tmin=0, Tmax=0, RAIN=0;

	public  Weather(){
		PAR = 1;
		date = 11111;
	}
	public void initalization() throws IOException
	{
		File wip=new File("WeatherInp.txt");
		FileReader fwip=new FileReader(wip);
	    BufferedReader dfwip=new BufferedReader(fwip);
	    String [] a2=new String [6];
		for (int i=0;i<6;i++) {
		a2[i]=dfwip.readLine();
		}
		Tmin=Double.parseDouble(a2[0]);
		Tmax=Double.parseDouble(a2[1]);
		SRAD=Double.parseDouble(a2[2]);
		RAIN=Double.parseDouble(a2[3]);
		//date=Integer.parseInt(a2[0]);
		PAR=Double.parseDouble(a2[5]);
		
		dfwip.close();
		
	}
	}
